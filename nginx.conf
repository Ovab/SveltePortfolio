worker_processes auto;

events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://portfolio-site;
        }

        location /api {
            proxy_pass http://portfolio-api;
        }

        # Add any additional locations or configuration as needed
    }
}
